from block import QC, Block

class Pacemaker:
    def setup(process):
        self.current_rond = 0
        self.last_round_tc = None
        self.pending_timeouts = 0

    def process_remote_timeout(tmo:Time):
        return None

    def advance_round_qc(qc:QC):
        if qc.vote_info.round < self.current_round:
            return False
        
        last_round_tc = None
        current_round = qc.vote_info.round + 1
        return True

